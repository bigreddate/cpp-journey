# Glossary · C++ 术语本

> 规则：术语记"英文 + 中文 + 一句话理解"；重要知识点单独成节。按天累积。

## 术语表

| 英文 | 中文 | 一句话理解 |
|---|---|---|
| statement | 语句 | 程序中的一条指令，以分号结尾 |
| function | 函数 | 一组按顺序执行的语句集合 |
| data | 数据 | 任何可以被计算机移动、处理或存储的信息 |
| value | 值 | 数据的具体实例（字母 a、数字 5、文本 Hello） |
| object | 对象 | 内存中的一块存储区域，有类型、能存值——**不管有没有名字** |
| variable | 变量 | **有名字的对象**（对象 ⊃ 变量） |
| identifier | 标识符 | 源代码里起的合法名字（变量名、函数名都是标识符） |
| data type | 数据类型 | 决定对象能存什么值、占多大内存 |
| integer (int) | 整数 | 不含小数部分的数 |
| initialization | 初始化 | **定义时**给初值（区别于事后的赋值 assignment） |
| assignment | 赋值 | 给**已存在**的变量换新值 |
| header file | 头文件 | 用 `#include` 引入，获得库中功能的声明 |
| iostream | 输入输出流库 | C++ 的 I/O 装备，提供 std::cout / std::cin（对位 C 的 stdio.h） |
| narrowing conversion | 收窄转换 | 会丢数据的隐式转换（double→int），花括号初始化会拒绝它 |
| list initialization | 列表初始化 | 用花括号 `{}` 的初始化语法（现代首选） |
| value initialization | 值初始化 | "没给初值时给默认值"的规则：内置类型→0，类→默认构造 |
| console | 控制台 | 控制台程序 stdio 依附的窗口（VS 弹出的黑窗，不是 cmd） |
| compile / link | 编译 / 链接 | .cpp→机器码是编译；拼成 .exe 是链接（LNK 开头的错=链接阶段） |
| warning / error | 警告 / 错误 | 警告=能跑但有隐患；错误=编译失败。已开 /W4 + /WX |

## 知识点笔记 · Day 2（2026-09-04，learncpp 1.1-1.5）

### 对象 / 变量 / 标识符（1.3）
- 标识符是源代码层面的名字；对象是内存层面的实体；变量 = 两者的交点
- 反例：`std::cout << 5 + 3;` 的临时结果 8 是对象但不是变量（没名字）
- 命名规则：字母/数字/下划线，不能数字开头，不能用关键字，区分大小写

### 初始化全家桶（1.4）
```cpp
int a = 5;    // 拷贝初始化（C 遗产）
int b(5);     // 直接初始化（老 C++ 风格）
int c{5};     // 列表初始化：现代首选，拒绝 narrowing
int d{};      // 空花括号 → 触发值初始化 → d = 0
```
- `int x = 4.5;` 能编译（悄悄截断成 4）；`int x{4.5};` 编译报错——花括号帮你拦数据丢失
- 口诀：**新代码一律 `{}`；`{}` 空着就是置零**
- 值初始化其他出没地（以后学）：`new int()`、`std::vector<int> v(100);`

### 输入输出（1.5）
- `std::cout <<` 输出，`std::cin >>` 输入，可连用串成一串
- 用 `std::string` 要 `#include <string>`——用什么就 include 什么
- ⚠️ 观察实验：cin 读 int 时输入字母 → 流进入失败状态，后续读取全部失灵（变量保持原值/为 0），原因以后会讲
- ⚠️ `std::cin >> name` 读到空格就停，带空格的名字要用别的办法（std::getline，以后学）

### IDE / 工程常识
- F5=调试运行（窗口一闪而过）；Ctrl+F5=直接运行（停住看输出）；F7=只编译不运行
- VS 弹的黑窗是控制台宿主（程序自己的 stdio 之家），不是 cmd；VSCode 输出在下方是因为程序复用了它的内置终端
- LNK1168 = 上次运行的黑窗没关，exe 被锁，关窗重编即可
- 项目属性已调：C++20 标准、/W4 警告等级、/WX 警告视为错误
- VS2026 新解决方案格式 .slnx（等价老 .sln）；.slnx/.vcxproj 要进 Git，编译产物不进

### 编译器的两个事实（0.x）
- 编译器只能检查"语法和规则"，查不出"逻辑不对"——跑起来对不对要靠自己测
- 警告不是建议，是"还没爆炸的错误"——/WX 逼着把每个警告修掉
